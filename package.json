{
  "name": "workspace",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "npm run dev --prefix vectorhub",
    "build": "cd vectorhub && npm ci && npm run build",
    "deploy": "cd vectorhub && npx wrangler pages deploy .next",
    "start": "npm run start --prefix vectorhub",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "openai": "^6.9.1"
  }
}