{
  "name": "workspace",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "npm run dev --prefix vectorhub",
    "build": "cd vectorhub && npm ci --legacy-peer-deps && npm run build",
    "deploy": "cd vectorhub && npx @cloudflare/next-on-pages && npx wrangler pages deploy .vercel/output/static",
    "start": "npm run start --prefix vectorhub",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "openai": "^6.9.1"
  }
}